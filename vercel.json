{
  "framework": "vite",
  "buildCommand": "cd artifacts/ascii-editor && rm -f pnpm-lock.yaml && pnpm install && pnpm run build",
  "outputDirectory": "artifacts/ascii-editor/dist",
  "installCommand": "cd artifacts/ascii-editor && rm -f pnpm-lock.yaml && pnpm install"
}
